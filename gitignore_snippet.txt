# ============================================================
# yeah_coding Skill — .gitignore 必要配置
# 将以下内容追加到你的项目 .gitignore 文件中
# ============================================================

# AI 自驱编码工作区（本地专用，禁止提交到版本库）
.auto_coding/

# 说明：
# .auto_coding/ 目录包含 AI 的工作状态文件（TRACKER.md、DISPATCH.md、任务文件等）
# 这些文件是本地专用的，不应进入版本库，否则会导致多人协作时状态冲突。
# 源码提交时 AI 会精确指定文件路径，不会误提交此目录。
